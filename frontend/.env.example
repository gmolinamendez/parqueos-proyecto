# En desarrollo no necesitas esto (Vite usa el proxy /api hacia localhost:8000)
# En producción, define la URL pública de tu backend terminada en /api
# Ejemplo: https://parku-tu-nombre.onrender.com/api
VITE_API_URL=
